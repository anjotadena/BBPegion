import {DefaultTheme} from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#BCD1E2',
    secondary: '#6D85A1',
    tint: '#FDFDFD',
    orange: '#CA3727',
    black: '#000000',
  },
};

export default theme;
